apiVersion: v1
kind: Service
metadata:
  name: timer-app-service
  namespace: timer-app
  labels:
    app: timer-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: timer-app
    track: blue
